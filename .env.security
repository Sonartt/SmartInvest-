# ===============================================
# SECURITY CONFIGURATION - SmartInvest Africa
# ===============================================
# This file contains security-critical environment variables
# DO NOT commit this file to version control
# DO NOT share these credentials

# CRITICAL: Admin Authentication (Required for security fixes)
ADMIN_USER=your_admin_email
ADMIN_PASS=your_secure_admin_password_here

# Application Environment
NODE_ENV=production
PORT=3000

# CORS Configuration - List allowed origins (comma-separated)
ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com,https://admin.yourdomain.com

# M-Pesa Configuration (for payment processing)
MPESA_ENV=sandbox
MPESA_CONSUMER_KEY=your_mpesa_consumer_key_here

MPESA_CONSUMER_SECRET=your_mpesa_consumer_secret_here
MPESA_BUSINESS_SHORTCODE=your_shortcode_here
MPESA_PASSKEY=your_passkey_here

# PayPal Configuration
PAYPAL_CLIENT_ID=your_paypal_client_id_here
PAYPAL_CLIENT_SECRET=your_paypal_client_secret_here
PAYPAL_WEBHOOK_ID=your_webhook_id_here

# Email Configuration (for notifications)
EMAIL_USER=your_email@gmail.com
EMAIL_PASS=

# File Upload Configuration
MAX_FILE_SIZE=52428800
UPLOAD_DIR=./uploads

# Database Configuration (for future migration)
DB_HOST=localhost
DB_USER=root
DB_PASS=
DB_NAME=smartinvest

# ===============================================
# SECURITY NOTES:
# ===============================================
# 1. Change ADMIN_USER and ADMIN_PASS to strong credentials
# 2. Set NODE_ENV=production for deployment
# 3. Use HTTPS for all communications
# 4. Whitelist ALLOWED_ORIGINS to your domain only
# 5. Use environment-specific secrets for each deployment
# 6. Rotate credentials regularly (every 90 days)
# 7. Store this file securely, never in version control
# 8. Use AWS Secrets Manager, HashiCorp Vault, or similar for production
# 9. Never log or display sensitive information
# 10. Implement audit logging for all admin actions

# ===============================================
# DEPLOYMENT CHECKLIST:
# ===============================================
# [ ] Change default credentials (CRITICAL)
# [ ] Set NODE_ENV=production
# [ ] Set ALLOWED_ORIGINS to your domain
# [ ] Enable HTTPS/SSL certificate
# [ ] Configure database credentials
# [ ] Set up monitoring and alerting
# [ ] Enable rate limiting (configured in code)
# [ ] Enable request logging
# [ ] Set up backup and recovery procedures
# [ ] Test all security fixes before deployment
# [ ] Document all security changes
# [ ] Schedule regular security audits (quarterly)
